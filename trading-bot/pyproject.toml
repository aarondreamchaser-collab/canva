[project]
name = "tradingbot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
live = ["ccxt>=4.0"]
dev = ["pytest>=7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
